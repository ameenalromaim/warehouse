<?php

return [

    /*
    |--------------------------------------------------------------------------
    | عنوان واجهة الـ API (للتطبيقات على الهاتف على نفس الـ Wi‑Fi)
    |--------------------------------------------------------------------------
    |
    | ضع هنا عنوان الجهاز الذي يشغّل Laravel مع المنفذ، مثال:
    | http://192.168.8.161:8000/api
    |
    | في Flutter استخدم نفس القيمة في ApiConfig.baseUrl (أو dart-define).
    |
    */

    'base_url' => env('API_BASE_URL', rtrim((string) env('APP_URL', 'http://localhost'), '/').'/api'),

    /*
     * مسار مشروع Flutter للمرجع (مرة واحدة في بيئتك).
     */
    'flutter_app_path' => env('FLUTTER_APP_PATH', ''),

];
